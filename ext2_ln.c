#include "ext2_helper.c"

int main(int argc, char **argv) {

    if(argc != 4) {
        fprintf(stderr, "Usage: ext2_ln <image file name> <first path> <second path>\n");
        exit(1);
    }


    return 0;
}
